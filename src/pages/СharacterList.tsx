import { Layout } from '@/shared';
import { CharacterCatalog } from '@/widgets';

export const CharacterList: React.FunctionComponent = () => {
  return (
    <Layout>
      <CharacterCatalog />
    </Layout>
  );
};
