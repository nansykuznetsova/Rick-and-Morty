import { Layout } from '@/shared';
import { CharacterDetailsContent } from '@/widgets';

export const CharacterDetails: React.FunctionComponent = () => {
  return (
    <Layout>
      <CharacterDetailsContent />
    </Layout>
  );
};
